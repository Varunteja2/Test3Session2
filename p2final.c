#include<stdio.h>
struct_fraction
{
  int num,den;
};
typedef_fraction Fraction;
fraction input_fraction()
{
  fraction f;
printf("enter one fraction(NUM DEN):\n");
scanf("%d%d,&f.num,&f.den");
  return f;
}
fraction smallest_fraction(fraction f1,fraction f2,fraction f3)
{
  flaot a = (flaot)f1.num/}
